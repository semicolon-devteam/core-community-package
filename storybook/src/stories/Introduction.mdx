import { Meta } from '@storybook/blocks';

<Meta title="Introduction" />

# @team-semicolon/community-core

<div style={{ 
  background: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)',
  color: 'white',
  padding: '2rem',
  borderRadius: '8px',
  marginBottom: '2rem'
}}>
  <h2 style={{ margin: 0, fontSize: '2rem', fontWeight: 'bold' }}>
    세미콜론 커뮤니티 플랫폼 코어 라이브러리
  </h2>
  <p style={{ margin: '0.5rem 0 0 0', fontSize: '1.1rem', opacity: 0.9 }}>
    재사용 가능하고 확장 가능한 React 컴포넌트 및 훅 시스템
  </p>
</div>

## 🎯 개요

**@team-semicolon/community-core**는 세미콜론 커뮤니티 플랫폼의 핵심 기능을 패키지화한 React 라이브러리입니다. 
개발자들이 빠르고 일관성 있게 커뮤니티 기능을 구현할 수 있도록 설계되었습니다.

## ✨ 주요 기능

### 🧱 Components
- **Atomic Design 패턴** 기반 컴포넌트 시스템
- **접근성(a11y)** 준수
- **TypeScript** 완전 지원
- **Tailwind CSS** 스타일링

### 🪝 Hooks
- **인증 시스템** (`useAuth`)
- **전역 상태 관리** (`useGlobalLoader`)
- **React Query** 통합 데이터 페칭
- **반응형 유틸리티** (`useDeviceType`)

### 🔧 Services
- **REST API** 클라이언트
- **인증 및 권한 관리**
- **파일 업로드 및 관리**
- **에러 핸들링 시스템**

### 🛠️ Utilities
- **날짜/시간 처리**
- **숫자 포맷팅**
- **이미지 최적화**
- **JWT 토큰 관리**

## 🚀 설치

```bash
# npm
npm install @team-semicolon/community-core

# yarn
yarn add @team-semicolon/community-core

# pnpm
pnpm add @team-semicolon/community-core
```

## 📋 빠른 시작

### 1. 패키지 초기화

```tsx
import { initializeCommunityCore } from '@team-semicolon/community-core';

initializeCommunityCore({
  apiUrl: process.env.REACT_APP_API_URL,
  supabase: {
    url: process.env.REACT_APP_SUPABASE_URL,
    anonKey: process.env.REACT_APP_SUPABASE_ANON_KEY,
  }
});
```

### 2. 컴포넌트 사용

```tsx
import { Button, Badge, Avatar, Input } from '@team-semicolon/community-core';

function MyComponent() {
  return (
    <div>
      <Avatar src="/profile.jpg" name="홍길동" size="md" status="online" />
      <Badge variant="success">활성</Badge>
      <Input label="이메일" type="email" placeholder="your@email.com" />
      <Button variant="primary" onClick={handleClick}>
        저장하기
      </Button>
    </div>
  );
}
```

### 3. 훅 사용

```tsx
import { useAuth, useGlobalLoader, usePostQuery } from '@team-semicolon/community-core';

function MyComponent() {
  const { user, loginWithLoader, isAdmin } = useAuth();
  const { withLoader } = useGlobalLoader();
  const { data: posts, isLoading } = usePostQuery({
    boardId: 1,
    page: 1,
    enabled: !!user
  });
  
  const handleSave = async () => {
    await withLoader(async () => {
      // API 호출
    });
  };

  return (
    <div>
      {isAdmin() && <AdminPanel />}
      {/* ... */}
    </div>
  );
}
```

## 📚 문서 구조

### Components
- **Atoms**: 기본 UI 요소들 (Button, Badge, Avatar, Input, Skeleton 등)
- **Molecules**: 조합된 컴포넌트들
- **Organisms**: 비즈니스 로직을 포함한 복합 컴포넌트들

### Hooks
- **Authentication**: 인증 관련 훅들
- **Data Fetching**: React Query 기반 데이터 페칭 훅들
- **Utilities**: 유틸리티 훅들

### Services
- **API Services**: REST API 클라이언트들
- **Authentication**: 인증 및 권한 관리
- **File Management**: 파일 업로드 및 관리

## 🎨 디자인 시스템

이 라이브러리는 일관성 있는 사용자 경험을 위해 통합된 디자인 시스템을 제공합니다:

- **색상 팔레트**: Primary, Secondary, Success, Warning, Error
- **타이포그래피**: Pretendard 폰트 기반 한국어 최적화
- **간격 시스템**: 8px 그리드 시스템
- **그림자와 테두리**: 계층감 있는 UI 요소들

## 🌟 주요 특징

- **🔧 개발자 친화적**: TypeScript 완전 지원으로 뛰어난 DX 제공
- **📱 반응형**: 모바일-퍼스트 접근 방식
- **♿ 접근성**: WCAG 2.1 가이드라인 준수
- **⚡ 성능 최적화**: Tree shaking 지원으로 번들 크기 최소화
- **🎨 커스터마이징**: Tailwind CSS 기반으로 쉬운 스타일 커스터마이징

## 🤝 기여하기

이 프로젝트에 기여를 환영합니다! 

- **GitHub**: [semicolon-devteam/community-core](https://github.com/semicolon-devteam/community-core)
- **Issues**: 버그 리포트나 기능 요청은 GitHub Issues에서
- **Pull Requests**: 개선사항이나 새로운 기능은 PR로 제출해 주세요

## 📄 라이선스

이 프로젝트는 MIT 라이선스 하에 배포됩니다.

---

<div style={{ 
  background: '#f8f9fa', 
  padding: '1.5rem', 
  borderRadius: '8px',
  borderLeft: '4px solid #667eea',
  marginTop: '2rem'
}}>
  <h3 style={{ margin: '0 0 0.5rem 0', color: '#667eea' }}>💡 시작하기</h3>
  <p style={{ margin: 0 }}>
    좌측 사이드바에서 각 컴포넌트와 훅들의 상세한 사용법을 확인해보세요. 
    각 스토리에는 실제 코드 예제와 인터랙티브 데모가 포함되어 있습니다.
  </p>
</div>