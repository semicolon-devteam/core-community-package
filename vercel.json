{
  "buildCommand": "npm run build:storybook",
  "outputDirectory": "storybook/storybook-static",
  "installCommand": "npm install && npm install --prefix storybook",
  "framework": null,
  "functions": {
    "**": {
      "maxDuration": 60
    }
  }
}